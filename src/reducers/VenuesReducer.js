import { combineReducers } from 'redux';
import { LOAD_VENUE_TIPS } from '../actions/actionTypes';

const venuesReducer = (state, action) => {
  switch (action.type) {
    case LOAD_VENUE_TIPS:
  }
};
